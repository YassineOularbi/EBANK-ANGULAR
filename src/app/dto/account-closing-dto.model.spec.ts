import { AccountClosingDto } from './account-closing-dto.model';

describe('AccountClosingDto', () => {
  it('should create an instance', () => {
    expect(new AccountClosingDto()).toBeTruthy();
  });
});
